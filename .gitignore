# Node
node_modules/
**/node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
package-lock.json.bak

# Next.js
.next/
out/
build/
*.tsbuildinfo

# Rust / Sails
target/
**/target/
*.rs.bk

# Indexer compiled output (Dockerfile rebuilds from src/)
services/indexer/dist/

# Worker compiled output + runtime state (Phase 4)
services/worker/dist/
services/worker/worker.state.json
services/worker/worker.history.jsonl

# Env files — never commit; allow templates (.env.*.example explicitly kept)
.env
.env.local
.env.*.local
.env.development
.env.production
!.env.example
!.env.*.example
!*.env.example

# OS / IDE
.DS_Store
.vscode/
.idea/
*.swp
*.swo

# Local logs / scratch
*.log
/tmp/

# Internal planning docs — moved to internal/
internal/

# Workflow-agent debug scratch (services/indexer/_*.mjs etc.)
services/indexer/_*.mjs
services/indexer/_*.ts

# Claude Code session conventions — kept local, never pushed
CLAUDE.md
AGENTS.md

# Checked-in SDK build artifact (consumed via file:-link)
!packages/sdk/dist/
!packages/sdk/dist/**
caldera/

# Codex visual-audit artifacts (screenshots from Playwright passes)
apps/web/.audit/
apps/web/.audit-*.png
